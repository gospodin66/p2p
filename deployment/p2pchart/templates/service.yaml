apiVersion: v1
kind: Service
metadata:
  name: {{ include "p2pchart.fullname" . }}
  labels:
    {{- include "p2pchart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "p2pchart.selectorLabels" . | nindent 4 }}
