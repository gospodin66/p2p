apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "p2pchart.fullname" . }}-test-connection"
  labels:
    {{- include "p2pchart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "p2pchart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
